# PostGRESQLPythonDemo
Demo DB with PostGRESQL and pyscopg2.

DB needs to be setup in pgAdmin prior to processing.

Change password parameters in "cred.txt" and "script1.py" to master password setup in pgAdmin.
